{% extends 'base.html.twig' %}

{% block title %}Edit Reservation{% endblock %}

{% block body %}
    <div class="container mx-auto p-6 rounded-lg shadow-md">
        <h1 class="text-2xl font-bold mb-6">Edit Reservation</h1>

        <div class="mb-4">
            {{ include('reservations/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="flex justify-between items-center mt-6 gap-4">
            <a href="{{ path('app_reservations_index') }}" class="text-amber-500 hover:text-amber-600 font-medium">Back to list</a>
            <div>
                {{ include('reservations/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}