{{ form_start(form, {'attr': {'class': 'space-y-6 bg-gray-50 p-8 rounded-2xl shadow-xl border border-gray-200'}}) }}
    <div class="grid grid-cols-1 gap-y-6">
        {{ form_widget(form, {'attr': {'class': 'block w-full px-5 py-3 border border-gray-300 rounded-lg shadow-sm focus:ring-amber-500 focus:border-amber-500 sm:text-base text-gray-700 placeholder-gray-400'}}) }}
    </div>
    <div class="flex justify-end">
        <button type="submit" class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-amber-500 to-orange-500 text-black font-semibold text-base leading-6 shadow-lg rounded-xl hover:from-amber-600 hover:to-orange-600 focus:outline-none focus:ring-4 focus:ring-amber-400 transition duration-300 ease-in-out">
            {{ button_label|default('Save') }}
        </button>
    </div>
{{ form_end(form) }}