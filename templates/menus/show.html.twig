{% extends 'base.html.twig' %}

{% block title %}Menu{% endblock %}

{% block body %}
    <h1>Menu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ menu.name }}</td>
            </tr>
            <tr>
                <th>NombrePersonne</th>
                <td>{{ menu.nombrePersonne }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_menus_index') }}">back to list</a>

    <a href="{{ path('app_menus_edit', {'id': menu.id}) }}">edit</a>

    {{ include('menus/_delete_form.html.twig') }}
{% endblock %}
